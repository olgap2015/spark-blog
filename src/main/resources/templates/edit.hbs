{{#partial "content"}}
    {{#if flashMessage }}
        <p id="flash-message">{{ flashMessage }}</p>
    {{/if}}
    <div class="edit-entry">
        <h2>Edit Entry</h2>
        <form action="/edit/{{ blogEntry.slug }}" method="post">
            <label for="title">Title</label>
            <input type="text" name="title" value="{{ blogEntry.postTitle }}"><br>
            <label for="entry">Entry</label>
            <textarea rows="5" name="entry">{{ blogEntry.postBody }}</textarea>
            <label for="tags">Tags (comma separated)</label>
            <input type="text" name="tags" value="{{#each blogEntry.tags}}{{ tagText }}&#44;&nbsp;{{/each}}">
            <input type="submit" value="Save Entry" class="button">
            <a href="/blogposts/{{ blogEntry.slug }}" class="button button-secondary">Cancel</a>
        </form>
        <form id="delete-entry" action="/delete/{{ blogEntry.slug }}" method="post">
            <input type="submit" value="Delete Entry" class="button button-secondary">
        </form>
    </div>
    </div>
{{/partial}}

{{#partial "title"}}Edit - "{{ blogEntry.postTitle }}"{{/partial}}

{{> base.hbs}}